import React from 'react';
import { TProfilingPanel } from '../store';

export const ProfilingPanel = (
  {
    panel
  }: {
    panel: TProfilingPanel
  }
) => {
  return <div>TProfilePanel panel</div>;
};
