import React from "react"
import ReactDOM from "react-dom/client"
import App from "@/App"
import "./index.css"

ReactDOM.createRoot(
  // @ts-ignore
  document.getElementById("root")
).render(<App />)
